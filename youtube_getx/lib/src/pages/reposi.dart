import 'package:flutter/material.dart';

class Reposi extends StatelessWidget {
  const Reposi({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('Reposi'),
      ),
    );
  }
}
